# Day 1

1. Read events from calendar
2. Start timer to send notification when event starts and ends

- Starts => start timer (display details) and end previous task, stop => stop -> in both cases,
  extend/shorten task if necessary

4. Add option to insert 1) event note, 2) re-insert timer, 3) view upcoming
   events, 4) view past events, 5) sync with calendar

5. Sync with calendar is like a factory reset - stop any running tasks then
   re-send notification

Start:

get tasks and sort into upcoming and past
